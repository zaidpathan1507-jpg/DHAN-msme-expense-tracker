// Deprecated: this module previously simulated a fake backend with
// localStorage and setTimeout. It has been replaced by the real FastAPI
// backend — see `src/lib/api.ts` for the real API client. This file is kept
// as an empty stub (rather than deleted) because this environment could not
// delete files on the linked device; nothing in the app imports it anymore.
export {}
