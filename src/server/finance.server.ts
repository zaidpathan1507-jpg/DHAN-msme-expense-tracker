// Deprecated: this module previously simulated server functions with an
// in-memory/localStorage fake backend. It has been replaced by the real
// FastAPI backend — see `src/lib/api.ts` for the real API client. Kept as an
// empty stub (rather than deleted) because this environment could not delete
// files on the linked device; nothing in the app imports it anymore.
export {}
